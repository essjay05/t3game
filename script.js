var $box = $('.box');
var player = 'X';
var clickedBoxes = [];
var xMoves = [];
var oMoves = [];
console.log($box);

$box.click(function() {
    var boxId = (event.target.id);
    alert('Clicked ' + boxId);
    // Check if boxId is in clickedBoxes array
     
        // if 

        // possibly use filter to create winning array
});